apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cowsay-ingresss
  namespace: ingress-nginx
spec:
  rules:
  - host: cowsay.devopsharshit.wuaze.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wisecow
            port:
              number: 4499

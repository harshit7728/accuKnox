apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cowsay-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: cowsay.devopsharshit.wuaze.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wisecow
                port:
                  number: 4499
